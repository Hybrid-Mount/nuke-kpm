# nuke-kpm

GPL-2.0-only KernelPatch module source for Hybrid Mount's `nuke_ext4_sysfs`.

## Build

Requirements:

- `AndroidPatch/kpm` kernel tree via `HYBRID_MOUNT_KP_DIR` or `KP_DIR`
- Android NDK, or `TARGET_COMPILE`

Build with:

```bash
make
```
